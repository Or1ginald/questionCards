import style from './Registration.module.scss';

import { ReturnComponentType } from 'types';

export const Registration = (): ReturnComponentType => (
  <div className="ff">
    <div className={style.main}>Регистрация</div>
  </div>
);
